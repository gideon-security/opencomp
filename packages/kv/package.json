{
  "name": "@gideon-defender/kv",
  "description": "Key-value store utilities backed by Redis for Comp AI",
  "version": "1.0.0",
  "dependencies": {
    "ioredis": "^5.4.1",
    "server-only": "0.0.1"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.7"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    },
    "./rate-limit": {
      "types": "./dist/rate-limit.d.ts",
      "import": "./dist/rate-limit.mjs",
      "require": "./dist/rate-limit.js"
    }
  },
  "files": [
    "dist/**",
    "README.md"
  ],
  "keywords": [
    "cache",
    "comp-ai",
    "key-value",
    "redis",
    "rate-limit"
  ],
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/comp-ai/comp.git",
    "directory": "packages/kv"
  },
  "scripts": {
    "build": "tsup src/index.ts src/rate-limit.ts --format cjs,esm --dts",
    "clean": "rm -rf .turbo node_modules dist",
    "dev": "tsup src/index.ts --format cjs,esm --watch --dts",
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "types": "./dist/index.d.ts"
}
